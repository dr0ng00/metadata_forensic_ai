# ============================================================================
# METAFORENSICAI - Project Dependencies
# ============================================================================

# --- 1. Image & Multimedia Processing ---
Pillow>=10.0.0
pillow-heif>=0.10.0
opencv-python>=4.8.0

# --- 2. Metadata Extraction & Provenance ---
exifread>=3.0.0
piexif>=1.1.3
# pyexiv2>=2.11.0  # Optional: Robust C++ binding for EXIF/IPTC/XMP

# --- 3. Machine Learning & Data Science ---
numpy>=1.24.3
pandas>=2.0.3
scipy>=1.11.0
scikit-learn>=1.3.0
joblib>=1.3.1
# torch>=2.0.0      # Optional: For deep learning features
# torchvision>=0.15.0 # Optional: Associated with torch

# --- 4. Utilities, Formatting & Output Generation ---
PyYAML>=6.0.1
reportlab>=4.0.0
Jinja2>=3.1.2
rich>=13.5.2
json5>=0.9.14
ImageHash>=4.2.0

# --- 5. System, Networking & Command-Line Interfaces (CLI) ---
click>=8.1.7
prompt-toolkit>=3.0.39
psutil>=5.9.0
requests>=2.28.0
tqdm>=4.65.0
tzlocal>=5.0.1
pytz>=2023.3
python-dateutil>=2.8.2

# --- 6. GUI Dashboard & Visualizations ---
streamlit>=1.20.0
plotly>=5.10.0

# ============================================================================
# EXTERNAL DEPENDENCY: ExifTool (RECOMMENDED)
# ============================================================================
# ExifTool is a platform-independent Perl library for reading/writing metadata.
# MetaForensicAI will use ExifTool when available for superior metadata extraction.
# 
# Installation:
#   Windows: Download from https://exiftool.org/
#            Rename exiftool(-k).exe to exiftool.exe and add to PATH
#   Linux:   sudo apt-get install libimage-exiftool-perl
#   macOS:   brew install exiftool
#
# The system will automatically fall back to Python-based extraction
# (Pillow + exifread) if ExifTool is not installed.
